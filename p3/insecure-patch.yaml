apiVersion: apps/v1
kind: Deployment
metadata:
  name: argocd-server
spec:
  template:
    spec:
      containers:
      - name: argocd-server
        args:
        - --insecure 

# apiVersion: kustomize.config.k8s.io/v1beta1
# kind: Kustomization
# resources: #-> the manifest that we want to customise
#   - https://raw.githubusercontent.com/argoproj/argo-cd/v3.4.2/manifests/install.yaml

# patchesStrategicMerge:
#   - insecure-patch.yaml